apiVersion: v1
kind: Service
metadata:
  name: {{ include "wishlist-helm.fullname" . }}
  labels:
    {{- include "wishlist-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wishlist-helm.selectorLabels" . | nindent 4 }}
